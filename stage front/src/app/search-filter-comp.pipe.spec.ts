import { SearchFilterCompPipe } from './search-filter-comp.pipe';

describe('SearchFilterCompPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFilterCompPipe();
    expect(pipe).toBeTruthy();
  });
});
